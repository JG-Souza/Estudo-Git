# Crie um repositório local e faça uma série de commits com mensagens descritivas. Clone um repositório remoto e faça um pull.

lista_numeros = [1, 2, 3, 4, 5]

def soma(*numeros):
    resultado = sum(numeros)

    return resultado

print(soma(*lista_numeros))
